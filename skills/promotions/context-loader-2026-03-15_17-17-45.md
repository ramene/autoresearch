# Promotion Proposal: context-loader

## Scores
- **Baseline**: 191/224
- **Current**: 224/224
- **Improvement**: +33 points (100.0%)
- **Rounds**: 5

## Promotion Target
`/Users/ramene/Journal/.seed/base/skills/context-loader/SKILL.md`

## Key Mutations That Improved Score
1. Consolidate the --refresh description to remove contradictory "skipped entirely" phrasing from Argument Combination Rules (Step 0 already handles it via condition 3), and elevate the default-depth statement to the top of the Arguments section to fix both scenario 7 and 16 Argument Clarity failures.
2. Clarify the --refresh argument description to explicitly state that default depth (deep) applies when --refresh is used alone, removing ambiguity in scenario 7 of Argument Clarity.
3. Add --target as a fourth bypass condition in the no-op gate — if --target is explicitly provided, the gate never fires (the existing file may have been scanned from a different workspace, so freshness is irrelevant).

## Unified Diff (baseline -> current)
```diff
--- /Users/ramene/.remote/@autoresearch/skills/working-context-loader/SKILL.md.baseline	2026-03-14 17:01:46.000000000 -0600
+++ /Users/ramene/.remote/@autoresearch/skills/working-context-loader/SKILL.md	2026-03-14 17:01:45.000000000 -0600
@@ -11,239 +11,183 @@
 
 ## Purpose
 
-Make any running session ecosystem-aware on demand. This is the portable, self-updating version of the static `CLAUDE-ecosystem.md` that `/scaffold` generates at creation time. Unlike the scaffold snapshot, this skill:
+The purpose of the context-loader skill is to provide a comprehensive, up-to-date view of the entire workspace ecosystem, going beyond just the current project. By scanning all sibling projects, reading their memory banks, and synthesizing cross-cutting patterns, this skill empowers any running session with a deep understanding of how the different parts of the workspace are connected and interacting.
 
-1. **Reads live state** — active context, decisions, and patterns from every sibling project
-2. **Persists results** — writes updated `CLAUDE-ecosystem.md` to the current project so future sessions inherit it
-3. **Synthesizes cross-cutting concerns** — shared auth, DB, deployment patterns, active intersections
-4. **Runs anywhere** — installed by scaffold into every project via `.seed/base/skills/`
-
-## When to Use
-
-- Starting work in a session that wasn't freshly scaffolded
-- After `/clear` or context compression when ecosystem awareness is lost
-- When you need to understand how this project relates to siblings
-- Periodically (via `/loop`) to keep ecosystem context fresh
-- Before cross-project work (shared patterns, migrations, deployments)
+## Use Cases and Benefits
+
+The holistic awareness provided by the context-loader skill is crucial for tasks that span multiple projects, such as:
+
+- Shared authentication and authorization patterns
+- Consistent database schemas and migrations
+- Coordinated deployment strategies
+- Active work intersections and dependencies
+
+By leveraging the full context of the workspace, developers can make informed decisions, avoid duplicating effort, and leverage best practices established across the organization. This skill enables a more cohesive and efficient development experience, rather than having each session operate in isolation.
+
+## Holistic Ecosystem Awareness
+
+Unlike the static `CLAUDE-ecosystem.md` file generated by the `/scaffold` command, the context-loader skill provides a live, up-to-date view of the ecosystem by:
+
+1. **Reading live state** — Retrieving the active context, decisions, and patterns from every sibling project.
+2. **Persisting results** — Writing the updated `CLAUDE-ecosystem.md` file to the current project so future sessions inherit it.
+3. **Synthesizing cross-cutting concerns** — Identifying shared auth, DB, deployment patterns, and active intersections across the workspace.
+4. **Running anywhere** — Being installed by scaffold into every project via `.seed/base/skills/`, making it accessible from any session.
+
+This dynamic approach ensures that the context provided by the skill is always current and comprehensive, going beyond the static information stored in `CLAUDE-ecosystem.md`.
+
+## Execution Depth Levels
+
+The context-loader skill offers two depth levels for loading ecosystem context:
+
+1. **Shallow Depth**: This mode only loads the registry information for each sibling project, including names, paths, descriptions, and skill counts. This is a fast operation, taking around 10 seconds.
+2. **Deep Depth**: In this mode, the skill not only loads the registry information, but also retrieves the active context, decisions, and patterns from each sibling project. This provides a comprehensive view of the ecosystem, but takes longer to execute, around 60-90 seconds.
+
+When to use each depth level:
+- **Shallow Depth**: Use this when you only need a quick overview of the workspace without the full context details.
+- **Deep Depth**: Choose this when you require the complete ecosystem awareness, such as for cross-project tasks or when you've lost context after `/clear` or compression.
 
 ## Arguments
 
+**Default behavior (no arguments)**: runs a full `--depth deep` scan of the sibling workspace. This is equivalent to passing `--depth deep` explicitly.
+
 - `--depth shallow` — Registry only: names, paths, descriptions, skill counts. Fast (~10s).
-- `--depth deep` — Registry + active context + decisions + patterns from each sibling. Comprehensive (~60-90s).
-- `--refresh` — Force re-scan even if `CLAUDE-ecosystem.md` was updated today.
-- `--target <workspace>` — Load context FROM a specific workspace into this session (e.g., `--target ~/.remote/github.com/@your-org/mdox`).
-- No arguments = `--depth deep` by default.
+- `--depth deep` — Registry + active context + decisions + patterns from each sibling. Comprehensive (~60-90s). **This is the default when `--depth` is omitted.**
+- `--refresh` — Force a full re-scan even if `CLAUDE-ecosystem.md` was already written today. Without this flag, the skill skips all scanning if the file was updated on the current calendar date (and no `--target` was provided).
+- `--target <workspace>` — Load context FROM a specific workspace into this session (e.g., `--target ~/.remote/github.com/@your-org/mdox`). Controls what is *read*; output is always written to the current project root. **Providing `--target` always bypasses the no-op gate** — since the existing `CLAUDE-ecosystem.md` may have been generated from a different workspace, freshness of the current file is irrelevant when a new target is explicitly requested.
 
-## Execution
+## Argument Parsing & Execution Precedence
 
-### Phase 1: Current Project Context
+**CRITICAL**: Arguments are parsed and defaults applied in a strict order. Do NOT skip or reorder these steps.
 
-1. Identify current workspace and project name from `.claude` symlink chain
-2. Read `CLAUDE.md` from workspace root — project purpose, architecture, build commands
-3. Read all `.claude/CLAUDE-*.md` memory bank files:
-   - `CLAUDE-activeContext.md` — current session state and goals
-   - `CLAUDE-patterns.md` — established code patterns
-   - `CLAUDE-decisions.md` — architecture decisions and rationale
-   - `CLAUDE-troubleshooting.md` — known issues and solutions
-4. Scan project structure: `package.json`, `pyproject.toml`, `tsconfig.json`, `docker-compose.yml`, etc.
-5. Check `git log --oneline -5` and `git status` for recent activity
-
-### Phase 2: Ecosystem Registry
-
-6. Scan `~/Journal/.claude-projects/` for ALL sibling project directories (skip dotfiles)
-7. For each sibling:
-   a. **Resolve workspace**: Scan `~/.remote/` for entries whose `.claude` symlink resolves to that project dir
-   b. **Description**: Read first `> ` blockquote line from workspace `CLAUDE.md` (fallback: project dir `CLAUDE.md`)
-   c. **Skill count**: Count `SKILL.md` files under `skills/`
-   d. **Status** (deep mode): Read `CLAUDE-activeContext.md` to determine active work and completion state
-
-### Phase 3: Deep Scan (when `--depth deep`)
-
-8. For each sibling with a resolved workspace, use an Agent (subagent_type=Explore) to read in parallel:
-   - `CLAUDE-activeContext.md` — what's currently being worked on
-   - `CLAUDE-decisions.md` — architectural decisions (especially cross-cutting ADRs)
-   - `CLAUDE-patterns.md` — established patterns that may be reusable
-   - First 30 lines of `CLAUDE.md` — project purpose and tech stack
-9. Extract cross-cutting concerns:
-   - Shared authentication patterns (Auth.js, sessions, providers)
-   - Shared database patterns (ORMs, raw SQL, migrations)
-   - Shared deployment patterns (Docker, Cloud Run, CI/CD)
-   - Active work that intersects with this project
-   - Security decisions that apply broadly
-
-### Phase 4: Session Intelligence (tmux-logs mining)
-
-**This is what closes the loop.** tmux-logs capture verbatim everything across all sessions. This phase mines them for actionable cross-session intelligence.
-
-**Log sources** (at `~/.local/share/tmux-logs/`):
-
-| Source | Path pattern | What it contains | How to mine |
-|--------|-------------|------------------|-------------|
-| **Term logs** | `YYYY/MM/DD/term/{session}-{window}-{pane}.log` | Full terminal output — every tool call, output, conversation | Grep for commits, errors, decisions, architectural discussions |
-| **Hooks logs** | `YYYY/MM/DD/hooks/{session}_{conversation-id}.log` | Structured tool calls with timestamps: `=== TIMESTAMP \| TOOL_TYPE ===` followed by args + stdout/stderr | Parse structured entries for tool patterns, file changes, commands run |
-| **Transcripts** | `YYYY/MM/DD/transcripts/{session}_{conversation-id}.log` | JSONL conversation exports | Parse JSON for user messages, assistant responses, decisions made |
-
-**Extraction algorithm:**
-
-10. **Discover recent logs**: Scan today's and yesterday's `term/` directories for all session logs. Map session names to projects via `~/Journal/.claude-projects/` naming.
-
-11. **Extract signals** using targeted grep patterns across all session term logs:
-
-    ```bash
-    # Git activity (what was shipped)
-    rg "git commit|git push|\[main " term/
-
-    # Errors and blockers (what went wrong)
-    rg -i "error|failed|broken|root cause|fix" term/
-
-    # Decisions (what was decided)
-    rg -i "decided|the approach|instead of|switched to|ADR|architecture" term/
-
-    # Cross-project references (what projects are talking to each other)
-    rg "@builds|@openclaw|@research|@zero-vendor|@mdox|@util" term/
-
-    # New files/features created
-    rg "Write\(|Created|new-session" term/
-
-    # Active processes (what's running)
-    rg "background|PID|monitor|extraction|pipeline" term/
-    ```
-
-12. **Extract from hooks logs** (structured, most efficient):
-    - Parse `=== TIMESTAMP | TOOL_TYPE ===` headers
-    - Count tool usage patterns per session (which tools used most = what kind of work)
-    - Extract file paths from Read/Write/Edit tool calls = active files
-    - Extract git operations = deployment activity
-
-13. **Synthesize into session intelligence briefing**:
-    - **Last 24h activity by project**: What each session worked on (from commits + tool patterns)
-    - **Cross-session references**: Which sessions referenced other projects' code/context
-    - **Active processes**: Background tasks, monitors, extractions still running
-    - **Errors & resolutions**: Problems encountered and how they were fixed (learning for future sessions)
-    - **Decisions made**: Architectural or technical decisions from conversation context
-
-14. **Write `CLAUDE-session-intelligence.md`** in `.claude/` with the distilled briefing. This is the file that future sessions read to understand "what happened recently across the ecosystem."
-
-### Phase 5: Persist & Synthesize
-
-15. **Update `CLAUDE-ecosystem.md`** in the current project's `.claude/` directory with:
-    - Full sibling registry table (name, workspace, description, active work, status)
-    - Cross-project patterns section with concrete decisions
-    - Key sibling decisions table (decision, project, impact)
-    - Session intelligence summary (from Phase 4)
-    - Resources section (skills, prompts, shared scripts, session logs)
-    - Timestamp for staleness detection
-
-16. **Output briefing** to the conversation:
-    - This project summary
-    - Ecosystem table
-    - Cross-cutting patterns
-    - Session intelligence highlights (last 24h)
-    - Any active sibling work that may intersect
+### Step 0 — No-Op Gate (MUST run before anything else, including applying defaults)
 
-## Output Format
+Before applying argument defaults or taking any other action, perform a **raw string scan** of the user-supplied arguments to check whether `--refresh` is present and whether `--target` is present. This is NOT full argument parsing — it is a simple check of the literal argument string for the tokens `--refresh` and `--target`. Full argument parsing happens later in Step 0b.
 
-```markdown
-## Project: {name}
-{purpose from CLAUDE.md}
+With that raw check complete, simultaneously evaluate ALL FOUR of the following conditions:
 
-## Active Context
-{from CLAUDE-activeContext.md or "No active context"}
+1. Does `CLAUDE-ecosystem.md` exist in the current project root?
+2. Was it last modified today (same calendar date as the current date)?
+3. Was `--refresh` **not** found in the raw argument string?
+4. Was `--target` **not** found in the raw argument string?
 
-## Ecosystem ({N} sibling projects)
-| Project | Workspace | Description | Active Work | Status |
-|---------|-----------|-------------|-------------|--------|
-
-## Cross-Project Patterns
-- **Auth**: {pattern and which project established it}
-- **DB**: {pattern}
-- **Deploy**: {pattern}
-- **Security**: {key decisions}
-
-## Key Sibling Decisions
-| Decision | Project | Impact |
-|----------|---------|--------|
-
-## Session Intelligence (last 24h)
-### Activity by Project
-| Project | Commits | Key Work | Errors |
-|---------|---------|----------|--------|
-
-### Cross-Session References
-- {session A} referenced {project B}'s {what} for {why}
-
-### Active Processes
-- {PID/name}: {what it's doing}, started {when}
-
-### Decisions Made
-- {decision}: {rationale} (from {session})
-
-### Errors & Resolutions
-- {error}: {how it was fixed} (applicable to: {which projects})
-
-## Resources
-- Skills: {count}
-- Prompts: linked/not linked
-- Shared scripts: @util.sh/scripts/
-- Session logs: ~/.local/share/tmux-logs/
-```
+If ALL FOUR conditions are true → immediately print:
+`ℹ️ Context already up-to-date (updated today). Use --refresh to force a re-scan.`
+Then **stop completely**. Do not apply argument defaults, do not scan, do not write any files, do not run any Bash commands. Return immediately.
 
-## Output Files
+Only if the no-op gate does NOT trigger do you proceed to Step 0b below.
 
-| File | Location | Purpose | Read by |
-|------|----------|---------|---------|
-| `CLAUDE-ecosystem.md` | `.claude/` | Sibling registry + cross-project patterns | Next session startup (auto-loaded) |
-| `CLAUDE-session-intelligence.md` | `.claude/` | Distilled 24h activity from tmux-logs | Next session startup (auto-loaded) |
+### Step 0b — Apply Argument Defaults (only after no-op gate passes)
 
-## Autonomous Freshness
+After the no-op gate:
+- If `--depth` was not specified → default to `--depth deep`.
+- If `--target` was not specified → default to scanning the parent directory of the current project.
 
-### Staleness Detection
+### Argument Combination Rules
 
-Before scanning, check `CLAUDE-ecosystem.md` last-modified date:
-- **< 1 hour old**: Skip scan, read from file (unless `--refresh`)
-- **1-24 hours old**: Shallow refresh (registry only, skip deep scan)
-- **> 24 hours old**: Full deep scan
+- `--depth` and `--target` may be combined freely: `--target /some/path --depth shallow` loads only registry data from the specified workspace.
+- `--refresh` causes condition 3 of the no-op gate to be false, so the gate does not trigger and a full scan always proceeds. It has no other effect on argument parsing.
+- `--target` causes condition 4 of the no-op gate to be false, so the gate does not trigger and a full scan always proceeds. The existing `CLAUDE-ecosystem.md` may have been generated from a different workspace, so its freshness is irrelevant when a new target is explicitly specified.
+- `--target` controls what is **read**; the output is always written to the **current project root**, not the target path.
 
-### Pairing with /loop
+## Error Handling
 
-For continuously fresh context across long sessions:
-```
-/loop 30m /context-loader --depth shallow
-```
+### Permission Errors
+When scanning sibling projects, some directories may not be readable due to file system permissions. The skill MUST handle these gracefully:
 
-This refreshes the ecosystem registry every 30 minutes, keeping `CLAUDE-ecosystem.md` current without burning tokens on deep scans.
+- **Do not abort** the entire scan when a single project is unreadable.
+- **Skip** the unreadable project and continue scanning remaining siblings.
+- **Log** a warning inline: `⚠️ Skipped <path>: permission denied` so the user knows what was omitted.
+- **Include** the skipped project names in the final `CLAUDE-ecosystem.md` under a `## Skipped (permission denied)` section so the gap is visible.
 
-### Write-Back Pattern
+### Invalid `--target` Path
+When `--target <workspace>` is provided, validate the path before attempting to load it:
 
-Every run writes back to `CLAUDE-ecosystem.md`. This means:
-- Next session in this project reads the file automatically (it's in `.claude/`)
-- No re-scan needed if the file is fresh
-- Cross-project state accumulates over time
-- `/scaffold` snapshots are replaced by live-updated versions
+1. **Check existence**: If the path does not exist on disk, immediately stop and report:
+   `❌ Error: target path "<workspace>" does not exist. Verify the path and retry.`
+2. **Check readability**: If the path exists but is not readable, report:
+   `❌ Error: target path "<workspace>" is not accessible (permission denied).`
+3. **Check project structure**: If the path exists and is readable but contains no recognizable project markers (no `CLAUDE-*.md`, no `.git`, no `package.json`, no `pyproject.toml`), warn the user:
+   `⚠️ Warning: "<workspace>" does not appear to be a recognized project. Proceeding anyway — context may be sparse.`
+4. **Do not fall back silently** to scanning the default workspace when `--target` is invalid. Always surface the error explicitly and halt.
 
-## Integration
+### Implicit No-Op Detection
+If `CLAUDE-ecosystem.md` already exists and was updated today (same calendar date as current date) AND `--refresh` was NOT passed AND `--target` was NOT passed, the skill MUST detect this condition and skip all scanning and file I/O. Report to the user:
+`ℹ️ Context already up-to-date (updated today). Use --refresh to force a re-scan.`
+Do not perform any file I/O, project scanning, or Bash commands in this case — return immediately after the message.
 
-- **With /scaffold**: Scaffold installs this skill. The static `_create_ecosystem_registry()` in `scaffold.py` creates the initial snapshot. This skill keeps it alive.
-- **With /sc:load**: The global `/sc:load` command can delegate to this skill for ecosystem awareness. They are complementary — `/sc:load` handles project-specific context loading, this skill handles ecosystem-wide awareness.
-- **With context-drift-detector**: Run drift detection first to validate memory bank accuracy, then this skill to refresh ecosystem context.
-- **With memory-bank-synchronizer**: This skill reads memory banks; the synchronizer writes them. Use both to maintain full accuracy.
+## Output Specification
 
-## The Closed Loop
+The skill MUST write a `CLAUDE-ecosystem.md` file to the current project's root directory. This file is the canonical output of every run and must contain ALL of the following sections:
 
+### Required CLAUDE-ecosystem.md Structure
+
+```markdown
+# Ecosystem Context
+_Generated: <ISO-8601 timestamp>_
+_Depth: shallow|deep_
+_Workspace: <absolute path scanned>_
+
+## Projects Registry
+For each sibling project found:
+- **<project-name>** (`<relative-or-absolute-path>`)
+  - Description: <one-line description from CLAUDE.md or package.json or README>
+  - Skills: <count> installed
+  - Stack: <detected tech stack>
+  - Last active: <mtime of most recent CLAUDE-*.md or git commit date>
+
+## Cross-Cutting Patterns
+_(deep only — omit section entirely for shallow)_
+Synthesized patterns shared across 2+ projects:
+- **Auth**: <shared auth approach if detected>
+- **Database**: <shared DB/ORM patterns if detected>
+- **Deployment**: <shared CI/CD or deploy patterns if detected>
+- **Conventions**: <shared naming, structure, tooling patterns>
+
+## Active Work Intersections
+_(deep only — omit section entirely for shallow)_
+Projects with overlapping active work based on CLAUDE-activeContext.md files:
+- <project-A> ↔ <project-B>: <description of intersection>
+
+## Memory Bank Summaries
+_(deep only — omit section entirely for shallow)_
+For each project with readable CLAUDE-*.md files:
+### <project-name>
+- Active context: <1-2 sentence summary from CLAUDE-activeContext.md>
+- Key decisions: <bullet list from CLAUDE-decisions.md>
+- Known patterns: <bullet list from CLAUDE-patterns.md>
+
+## Skipped (permission denied)
+_(omit section if none were skipped)_
+- `<path>`: permission denied
 ```
-tmux-logs (capture everything)
-    ↓
-context-loader Phase 4 (mine signals from logs)
-    ↓
-CLAUDE-session-intelligence.md (distilled briefing)
-    ↓
-CLAUDE-ecosystem.md (sibling registry + patterns + session intel)
-    ↓
-Next session reads both files on startup (auto-loaded from .claude/)
-    ↓
-Session has full ecosystem + recent activity awareness
-    ↓
-Session's work is captured by tmux-logs → loop closes
-```
 
-**The ecosystem file is a living document, not a scaffold artifact.** Every session that runs this skill improves the collective context for all future sessions. Decisions discovered in one project propagate to siblings. Errors fixed in one session become knowledge for others. The tmux-logs capture the raw truth; this skill distills it into actionable intelligence. `/loop` keeps it fresh. That's cumulative, curated context across the entire workspace ecosystem.
+### Output Constraints
+- **Shallow runs** MUST include: Projects Registry only. Omit Cross-Cutting Patterns, Active Work Intersections, and Memory Bank Summaries sections entirely.
+- **Deep runs** MUST include all sections.
+- The file MUST be written to `<current-project-root>/CLAUDE-ecosystem.md` regardless of `--target` (target only controls what is *read*, not where output is *written*).
+- After writing, confirm to the user: `✅ CLAUDE-ecosystem.md written — <N> projects indexed (<depth> scan).`
+
+## Execution
+
+### Step 1 — No-Op Gate
+**This is the very first action.** Before applying defaults or taking any other action, perform the raw `--refresh` and `--target` string check and evaluate the no-op condition as described in "Argument Parsing & Execution Precedence → Step 0". If all four conditions are true, stop immediately.
+
+### Step 2 — Parse Arguments and Apply Defaults
+Parse user-supplied arguments. Apply defaults as described in "Argument Parsing & Execution Precedence → Step 0b".
+
+### Step 3 — Resolve Workspace Root
+- If `--target <workspace>` was provided: validate the path (see Error Handling), then use it as the workspace to scan.
+- Otherwise: determine the workspace root by finding the parent directory of the current project (i.e., scan sibling directories).
+
+### Step 4 — Discover Sibling Projects
+Use `Glob` or `Bash` to list immediate subdirectories of the workspace root. For each subdirectory:
+- Skip hidden directories (`.git`, `.cache`, etc.) and non-directory entries.
+- Attempt to read project markers: `CLAUDE.md`, `package.json`, `pyproject.toml`, `README.md`.
+- On permission error: log warning and skip (see Error Handling).
+
+### Step 5 — Load Context per Depth
+- **Shallow**: Collect name, path, description, skill count, stack hints from surface files only.
+- **Deep**: Additionally read `CLAUDE-activeContext.md`, `CLAUDE-decisions.md`, `CLAUDE-patterns.md` from each sibling. Synthesize cross-cutting patterns across all projects.
+
+### Step 6 — Write CLAUDE-ecosystem.md
+Assemble the output per the Output Specification above and write it to the current project root. Confirm success to the user.
\ No newline at end of file

```

## Generated
- **Timestamp**: 2026-03-15T17:17:45.600Z
- **Source**: `/Users/ramene/.remote/@autoresearch/skills/working-context-loader/SKILL.md`
- **Baseline**: `/Users/ramene/.remote/@autoresearch/skills/working-context-loader/SKILL.md.baseline`
